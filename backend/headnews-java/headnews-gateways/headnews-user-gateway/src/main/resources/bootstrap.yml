spring:
  application:
    name: user-gateway
  cloud:
    consul:
      hostname: 127.0.0.1
      port: 8500
      discovery:
        enabled: true
        service-name: ${spring.application.name}
    gateway:
      routes:
        - id: user-service-route
          uri: lb://user-service
          predicates:
            - Path=/user-service/**
server:
  port: 18080
